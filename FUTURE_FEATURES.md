# Future Features

## Text-to-Speech

- ~~**Local TTS engine**~~ — implemented with Piper (`en_US-amy-medium`), fully offline

## MCP Server

- **Delete via MCP** — expose `delete_paper` and `delete_folder` as MCP tools
- **Rename via MCP** — expose `rename_paper` as an MCP tool
